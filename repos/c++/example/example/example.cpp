


#include <iostream>

void main()
{






    /*float first = 0;
    float second = 0;

    std::cout << "Enter the first number: " << std::endl;
    std::cin >> first;

    if (std::cin.fail()) {
        std::cout << "You didn't enter an integer. We'll just use '5' instead."
            << std::endl;
        first = 5;
        std::cin.clear();
        std::cin.ignore(std::cin.rdbuf()->in_avail());
    }

    std::cout << "Enter the second number: " << std::endl;
    std::cin >> second;

    if (std::cin.fail()) {
        std::cout << "You didn't enter an integer. We'll just use '8' instead."
            << std::endl;
        second = 8;
        std::cin.clear();
        std::cin.ignore(std::cin.rdbuf()->in_avail());
    }

    if (second != 0) {

        std::cout << first << " / " << second << " = " << (first / second) << std::endl;
        std::cout << first << " * " << second << " = " << (first * second) << std::endl;
        std::cout << first << " % " << second << " = " << ((int)first % (int)second) << std::endl;
    }
    else {
        std::cout << first << " * " << second << " = " << "0" << std::endl;
        std::cout << first << " % " << second << " = " << first << std::endl;
    }
    std::cout << first << " + " << second << " = " << (first + second) << std::endl;
    std::cout << first << " - " << second << " = " << (first - second) << std::endl;
        system("pause");
        std::cin.ignore(std::cin.rdbuf()->in_avail());
        std::cin.get();*/



}

